<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RecipeApp" parent="Base.Theme.RecipeApp">
        <item name="android:textColor">@color/defaultLightColor</item>
        <item name="android:statusBarColor">@color/defaultLightColor</item>
        <item name="colorPrimary">@color/defaultLightColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="toolbarStyle">@color/defaultLightColor</item>
    </style>

</resources>
